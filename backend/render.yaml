
services:
  - type: web
    name: CS49200_ML_Recipe_Generator
    env: python
    rootDir: ./backend
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host=0.0.0.0 --port=$PORT"
    plan: starter
    envVars:
      - key: PORT
        value: 8000
